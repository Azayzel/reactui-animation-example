import React, { useState, useEffect } from "react";
import { useLocation } from "react-router-dom";

const Home = (): JSX.Element => {
  return (
    <a className="App-link" href="/animations">
      <h1>CH Robinson Animations</h1>
    </a>
  );
};

export default Home;
